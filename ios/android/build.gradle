buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        // Make sure the version here matches the version compatible with your Gradle version
        classpath 'com.android.tools.build:gradle:8.2.1'  // The Android Gradle Plugin version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
